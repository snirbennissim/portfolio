# GitHub username (defaults to 'snirbennissim')
VITE_GITHUB_USERNAME=snirbennissim

# Optional: GitHub personal access token to increase API rate limit (60 → 5000 req/hr)
# Generate at: https://github.com/settings/tokens (read:public_repo scope only)
VITE_GITHUB_TOKEN=
